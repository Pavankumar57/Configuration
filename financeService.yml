server:
  port: 8020


eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://client:client@localhost:8030/eureka/
  instance:
    hostname: localhost